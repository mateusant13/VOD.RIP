[pytest]
asyncio_mode = auto
testpaths = tests
# Live network/env suites (test_*_real*.py) are opt-in: run with `pytest -m real`.
addopts = -m "not real"
markers =
    real: live network/environment tests (YouTube/Twitch/Kick/CDN, tokens) — opt-in
